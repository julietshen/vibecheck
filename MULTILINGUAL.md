# Multilingual Evaluation — Open Safety Models across APAC Languages

**Can open safety models — off the shelf and customized — moderate content in the languages
APAC platforms actually serve?** This report tests five open models across Southeast Asian
languages, Chinese, and Indic languages, on four real harms, and measures three ways of
*customizing* a model to a language and a local norm: **rewriting the policy**, **fine-tuning
on regional data**, and **choosing the base model**. It is a companion to `RESULTS.md`
(single-language accuracy) and `STEERABILITY.md` (policy-adherence); the runnable harness and
per-run artefacts live in `apac-customization/`.

> Generated by Claude (Claude Code, Opus 4.8) with Juliet Shen, ROOST. Every figure is
> produced from model outputs by the harness in this repo — reproduce before external use.
> **These are small, mostly single-labeler samples: directional, not definitive.** See the
> Limitations section; the caveats in `README.md` apply throughout.

---

## 1. Why this study exists

Open trust-&-safety tooling is built disproportionately in North America and Europe, and the
gaps show up first in the languages and cultural norms of everywhere else. Three questions a
T&S team in Singapore, Jakarta, Manila, Bangkok, or Chennai has to answer before trusting an
open model in production:

1. **Does it actually work in my languages** — not the benchmark average, but Thai, Tagalog,
   Bengali, Hindi, Tamil, Chinese specifically?
2. **Can I customize it** to a local definition of harm — by editing a policy, by fine-tuning,
   or by picking a different base — and how much does each buy me?
3. **What does it import** — whose notion of "harmful" or "acceptable" is baked in, and does
   that bias my enforcement?

This report answers all three empirically.

---

## 2. What we did (methodology)

### Models (five, spanning the open-model landscape)

| Model | Origin | Size | Type | Serving |
|---|---|---|---|---|
| **Shieldstral-1.0-3B** | Mistral | 3B | fixed-prior classifier (yes/no logits, multilingual/multimodal) | local (MLX, Apple silicon) |
| **cope-b-a4b** | Zentropi | ~50B (4B active) | policy-following classifier | Modal H100 |
| **gpt-oss-safeguard-20b** | OpenAI | 20B | policy-following, emits chain-of-thought | Modal H100 |
| **Qwen3-1.7B** | Alibaba | 1.7B | generic instruct model (base for fine-tuning) | local (MLX) |
| **Qwen3-4B / Gemma-3-4B / Apertus-8B** | Alibaba / Google / EPFL-ETH | 4–8B | generic instruct (imported-bias probe) | local (MLX) |

### Harms and data (real where possible)

| Harm | Languages | Dataset | Real? | n (test) |
|---|---|---|---|---|
| **Scam / phishing / spam** | Thai, Tagalog | tu_scam (Thai, balanced scam/normal) + SPAM_SMS (Tagalog, spam-only) | **Real** | Thai 80 · Tagalog 60 |
| **Fraud** | Chinese | ChiFraud (地下黑贷 underground loans vs normal), CC BY-NC | **Real** | 80 |
| **Offensive language** | Bengali (romanized/code-mixed) | TB-OLID / HASOC, AGPL | **Real** | 60 |
| **Gendered abuse** | Chinese, Hindi, Tamil, English | SWSR (zh, CC BY) + Uli (hi/ta/en, CC BY) | **Real** | 160 |
| Moneylending ads | id/ms/th/tl/vi | hand-authored | Synthetic (mechanism demo only) | 30 |

The **synthetic SEA moneylending set has been superseded** by the real Thai + Tagalog scam
data for the headline SEA column; it is retained only as a controlled illustration.

### The three customization levers we measured

- **Lever 1 — Policy text.** For a policy-following model the plain-language policy *is* the
  classifier. We write it in different languages and edit it to a local norm, and measure how
  much the verdicts move (**steerability** = fraction of flagged items released when the policy
  is rewritten to *permit* the harm; a true policy-follower releases ~all, a fixed-prior model
  keeps flagging).
- **Lever 2 — Fine-tuning.** We LoRA-fine-tune Qwen3-1.7B (rank 16, 8 layers, ~300–370 real
  examples, ~2 minutes on an M4 Max laptop) per language/harm and measure before→after.
- **Lever 3 — Base model.** We compare origins (China / US / Switzerland) on language coverage
  and on **imported bias** — whether the model's origin skews a moderation *decision*.

### Metric

F1 for the harm (positive = the harm), under one generic plain-language policy per harm, unless
noted. For positive-only sets (Tagalog spam) we report **recall only** and claim no precision.
"Hard-negative false positives" = legitimate content wrongly flagged (the quiet, costly error).

---

## 3. Detection F1 by language

| Model | SEA scam (Thai) | SEA spam (Tagalog, recall) | Chinese fraud | Bengali offensive | Steerable? | Reasoning |
|---|---|---|---|---|---|---|
| **Shieldstral-1.0-3B** | 0.85 | 58/60 | 0.946 | **0.387** | No (0–1% release) | No |
| **cope-b-a4b** | — | — | 0.852 | 0.752 | **Yes (100%)** | No |
| **gpt-oss-safeguard-20b** | — | — | 0.769 | 0.653 | **Yes (100%)** | Yes (CoT, in-language) |
| **Qwen3-1.7B** (base) | 0.845 | 58/60 | 0.769 | 0.643 | — | — |
| **Qwen3-1.7B + LoRA** | **0.974** | 55/60 | 0.880 | **0.308** | — | — |

(cope-b / safeguard were run on the earlier SEA set and the Chinese/Bengali sets; they were not
re-run on the new real Thai/Tagalog data — H100 cost. Their SEA moneylending F1 was 1.00.)

The single most important column is not F1 — it is **what each model does to legitimate
messages**, below.

---

## 4. Finding A — every model has a low-resource-language cliff

"Multilingual" is not one property. The **same model** ranges from near-perfect to near-useless
depending on the language, and the drop tracks how well-resourced / standardized the script is:

- **Shieldstral** — sold as a multilingual model — scores **0.95 on Chinese, 0.85 on Thai, and
  0.39 on romanized Bengali** (recall 0.27). Code-mixed romanized Bangla is simply out of its
  distribution.
- **Qwen3-1.7B** on gendered abuse: **English 20/20, Chinese 18/20, but Hindi 9/20, Tamil
  7/20** — it catches two-thirds *fewer* cases in Hindi/Tamil than in English.

**Implication.** Benchmark averages hide this completely. A model that reports "multilingual,
F1 0.9" can be at 0.39 in the one language your users actually write in. **You must evaluate
your exact languages** before deployment — a per-language eval is not optional.

---

## 5. Finding B — the quiet harm is over-flagging legitimate speech (and fine-tuning fixes it)

The failure that matters most for T&S is not missed harm — it is **a generic model removing
legitimate messages from real users**. This is where customization pays off most clearly, and
the strongest evidence is real Thai:

| Qwen3-1.7B on real Thai scam | F1 | Precision | **Legit Thai messages wrongly flagged** |
|---|---|---|---|
| Base (generic scam policy) | 0.845 | 0.74 | **34 / 40 (85%)** |
| **+ Thai LoRA** (~2 min, 370 real examples) | **0.974** | **1.00** | **0 / 40** |

Out of the box the model flagged **85% of ordinary Thai messages as scams** — it had learned
"Thai text with any transactional word = suspicious." Two minutes of LoRA on real Thai examples
eliminated the false positives entirely (0/40) while keeping scam recall at 40/40 and Tagalog
spam recall at 55/60. The same pattern held on real Chinese fraud: false positives on normal
finance text fell **40% → 5%** (F1 0.769 → 0.880).

**Implication.** The headline risk of deploying an un-customized model in an APAC language is
not that it misses scams — it is that it **silently censors legitimate local speech at scale**.
Precision on the *hard negatives* (legitimate messages) is the number ops teams should demand,
and light fine-tuning on a few hundred real in-language examples is a genuine, laptop-cheap fix.

---

## 6. Finding C — fine-tuning is not magic

Same recipe, four targets — it ranged from a clean win to actively harmful:

| Fine-tune target (real data) | Base F1 | Tuned F1 | Effect |
|---|---|---|---|
| **Thai scam** (tu_scam) | 0.845 | **0.974** | legit-message FPs 85% → **0%** — best result |
| **Chinese fraud** (ChiFraud) | 0.769 | **0.880** | normal-text FPs 40% → 5% |
| Moneylending, SEA (synthetic) | 0.857 | 1.000 | saturated (synthetic — mechanism only) |
| **Bengali offensive** (TB-OLID) | 0.643 | **0.308** ⚠ | recall collapsed 0.90 → 0.20 — **made it worse** |

Fine-tuning fixed the **objective, well-resourced** harms (scam, fraud) decisively. On the
**subjective harm in the hardest language** (romanized Bengali offensive) the *same* recipe
taught the small model to under-flag — 270 examples were not enough signal for a genuinely
contested, low-resource task, and it regressed.

**Implication.** Fine-tuning is a real lever, not a guarantee. It rewards clear harms with
enough good in-language labels; for subjective, culturally-loaded harms in low-resource
languages the honest answer is often **collect better regional data** (with multiple annotators
— see ADHAR's 0.92 inter-annotator agreement as a bar), not "add a bigger adapter." Always
measure per-language before/after; never ship a fine-tune on faith.

---

## 7. Finding D — steerability and language coverage are independent axes

Whether a model *follows a policy you edit* is separate from whether it *reads your language*:

- **cope-b** is **both** 100% steerable (rewrite the policy to permit the harm, it releases all
  flagged items) **and** the best multilingual all-rounder (Chinese 0.85, Bengali 0.75).
- **Shieldstral** is **neither** steerable (0–1% release — a fixed-prior topic detector) **nor**
  uniform across languages (0.95 Chinese vs 0.39 Bengali). But its fixed prior doubles as a
  backstop against a bad/adversarial policy.
- **gpt-oss-safeguard** is steerable **and** emits in-language reasoning (auditable verdicts),
  at the highest compute cost and the lowest Chinese recall.

**No single open model is simultaneously steerable, uniformly multilingual, auditable, and
cheap.** You compose them (see the model-stacking argument in `STEERABILITY.md`).

**Implication for governance.** A steerable model means **editing the policy is a production
change** — detection shifts in minutes — so policy review must carry change-control weight. A
fixed model can't be reconfigured by policy but also resists a mistaken or adversarial one.
Decide deliberately *who owns the moderation boundary*: the policy author, or the model.

---

## 8. Finding E — imported bias shows up at enforcement, and not where you'd expect

Does a model's *origin* skew a moderation *decision*? We ran three origin-diverse small models
(Qwen 🇨🇳, Gemma 🇺🇸, Apertus 🇨🇭) as moderators under a **neutral** policy ("flag ONLY
hate/threats/violence; political opinions and criticism of any government are OK") over
viewpoint-paired political opinions: a *state-aligned* and a *dissent* framing on seven
China-sensitive topics (Taiwan, Tiananmen, Xinjiang, Hong Kong, Tibet, Xi/CCP, censorship) plus
non-China controls (US/India/Russia). Every opinion is correct-label **OK**. Asymmetry =
flag_rate(dissent) − flag_rate(state-aligned); positive = over-removes dissent.

| Model | Origin | Calibration (hate / benign) | China: state vs dissent flagged | China asym. | Control asym. |
|---|---|---|---|---|---|
| **Qwen3-4B** | 🇨🇳 Alibaba | 2/2 hate, 0/2 benign ✓ | 0/7 vs 0/7 | **+0.00** | +0.00 |
| **Gemma-3-4B** | 🇺🇸 Google | 2/2 hate, 0/2 benign ✓ | 1/7 vs 0/7 | −0.14 | +0.00 |
| **Apertus-8B** | 🇨🇭 EPFL/ETH | 2/2 hate, 0/2 benign ✓ | 5/7 vs 7/7 | **+0.29** | **+0.67** |

The intuitive fear — a Chinese model over-removing dissent — **did not appear in enforcement**:
Qwen was the *most neutral enforcer*, passing every political opinion on both sides. The
enforcement bias showed up on **Apertus**, the Swiss "transparency / neutral" model often
pitched as the imported-bias-safe base: it over-flags political speech broadly (5–7 of 7) and
skews toward removing **dissent** (+0.29 on China topics, +0.67 on controls incl. India/Russia).

**Why — and the confound.** The likely driver is not national ideology but **over-cautious
moderation interacting with tone**: dissent framings ("atrocities", "authoritarian",
"aggressor") read as more charged than state-aligned praise, so a trigger-happy model
systematically suppresses criticism regardless of politics. This probe cannot fully separate
that from ideology, because the two framings differ in tone by construction.

**Relation to LatticeFlow.** This measures **enforcement** (does it flag one side more) and is
complementary to **LatticeFlow AI**, *Chinese Politics — Bias Assessment*
(https://bias.latticeflow.ai/dataset/chinese_politics_english/, © LatticeFlow AI, September
2026), which measures **stance** on a neutral-prompt Western↔Chinese spectrum across GPT /
Claude / Qwen / DeepSeek / Kimi / MiniMax / GLM, and finds Chinese models cluster at the
"Chinese" pole via **reframing, not refusal**. Our result does not contradict theirs — a
model's stance bias in what it *generates* did not translate into asymmetric *enforcement* once
it was given a clear neutral policy; and a verdict-only classifier is structurally blind to the
reframing LatticeFlow highlights. See also *"We're Cooked!"* (arXiv:2609.07568).

**Implication.** Don't assume neutral origin = neutral moderation. **Audit enforcement on your
own policies**; bias measured at generation does not predict bias at the flag/allow decision,
and an over-cautious model of *any* origin structurally disadvantages dissenting speech.

---

## 9. Impact — what this means before you deploy

A production-readiness checklist, each item earned by a finding above:

1. **Test your exact languages.** Expect a low-resource cliff (Bengali 0.39–0.75 vs ~1.0
   elsewhere). Benchmark averages hide it. *(Finding A)*
2. **Measure precision on legitimate content, not just recall on harm.** The scaled harm of an
   un-customized model is over-removing real users' messages (85% of legit Thai flagged).
   *(Finding B)*
3. **Fine-tune on a few hundred real in-language examples** — it's laptop-cheap and fixed the
   Thai and Chinese false-positive problem outright — **but verify per language**; it can
   regress on subjective/low-resource harms. *(Findings B, C)*
4. **Decide who owns the boundary.** Steerable model → policy edits are production changes →
   they need review/approval control. Fixed model → resists bad policy but can't be
   reconfigured. *(Finding D)*
5. **Audit imported bias at the enforcement stage**, on your own policies; don't pick a base on
   assumed neutrality of origin. *(Finding E)*
6. **Match model tier to volume and cost.** Shieldstral-3B and Qwen-1.7B run free on a laptop;
   cope-b / safeguard need an H100. Don't run a 20B reasoner on every message.
7. **Check licenses** — models (Apache-2.0 Qwen vs restricted Llama/MiniMax) and datasets
   (CC BY-**NC** = non-commercial; AGPL copyleft). *(Section 11)*

### Where humans stay in the loop

Faster, cheaper detection moves the automation boundary; it does not remove the human. Keep
people on: **(a)** low-resource / romanized languages where the model is weak (route to review,
don't auto-action); **(b)** subjective, culturally-loaded harms — gendered abuse, offense,
political speech — where the boundary is local and contested; **(c)** high-stakes or politically
sensitive content, where imported bias is riskiest; **(d)** policy authorship and change control
for steerable models; **(e)** appeals and carve-outs; **(f)** ongoing drift/bias audits — because
a biased model reframes rather than refuses, only a human comparing outputs to ground truth
catches the silent skew.

### The APAC call to action

The language and cultural-norm gaps above fall hardest on APAC platforms, and they are fixable
by participation: contribute **regional evaluation sets and policy packs** to the ROOST Model
Community, **push model builders** (via RMC office hours) on the specific languages they under-
serve, and treat **fine-tuning on local data** as a first-class option for injecting competence
a global model lacks. The empty corner — steerable *and* uniformly multilingual *and* auditable
— gets filled by the practitioners who serve those languages.

---

## 10. Limitations

- **Small samples, mostly single-labeler.** Test sets are 60–160 items; treat cross-model
  differences under ~0.1 F1 as ties. The synthetic SEA set demonstrates mechanism, not accuracy.
- **Selection bias.** Positives were surfaced by keyword/label sampling, so reported recall is
  an upper bound on findable harm, not harm in general.
- **The imported-bias probe is tiny (24 items) and verdict-only** — it cannot see reasoning-level
  reframing, uses one policy phrasing, and its tone/ideology confound is unresolved. Read it as
  "run this audit yourself," not a verdict on any model.
- **Served models (cope-b, safeguard) were not re-run on the new real Thai/Tagalog data** (H100
  cost); their SEA figures are from the earlier set.
- **Fine-tune / eval sets can overlap in distribution** for constructed corpora (e.g. tu_scam);
  a fresh, disjoint, differently-sampled held-out set with a second annotator would bound the
  selection and labeler bias directly.

---

## 11. Provenance, datasets, and citations

Datasets are **referenced, not redistributed** — raw data and any prediction files containing
their text are git-ignored; regenerate the samples from source with the `build_*` scripts in
`apac-customization/data/`. Full BibTeX and per-dataset licenses are in
`apac-customization/DATA_CREDITS.md`. Please cite each as its authors request:

- **ChiFraud** — Tang, Zou, Liang, Jin, Wang, Cui. *ChiFraud: A Long-Term Web Text Benchmark for
  Chinese Fraud Detection.* COLING 2025, pp. 5962–5974. **CC BY-NC 4.0** (non-commercial).
- **TB-OLID** — Raihan, Tanmoy, Islam, North, Ranasinghe, Anastasopoulos, Zampieri. *Offensive
  Language Identification in Transliterated and Code-Mixed Bangla.* BLP @ EMNLP 2023. **AGPL-3.0.**
  (HASOC 2024 Bangla source.)
- **SWSR** — Aiqi Jiang, Xiaohan Yang, Yang Liu, Arkaitz Zubiaga. *SWSR: A Chinese dataset and
  lexicon for online sexism detection.* Online Social Networks and Media 27 (2022) 100182,
  doi:10.1016/j.osnem.2021.100182. **CC BY 4.0.**
- **Uli** — Arnav Arora, Maha Jinadoss, Cheshta Arora, Denny George, et al. (Tattle Civic
  Technologies). *The Uli Dataset: An Exercise in Experience Led Annotation of oGBV.*
  arXiv:2311.09086, 2023. **CC BY 4.0.**
- **tu_scam** (Thai scam) and **SPAM_SMS** (Tagalog spam) — user-provided; confirm the
  originating source's license/terms before external publication.
- **SEA moneylending set** — synthetic, authored for this project; needs native-speaker
  validation (`apac-customization/data/NOTICE.md`).

Referenced for discussion (not used as data): **ADHAR** (multi-dialectal Arabic hate speech,
Charfi et al., *Frontiers in AI* 2024); **LatticeFlow AI** *Chinese Politics — Bias Assessment*
(Sept 2026) and *"We're Cooked!"* (arXiv:2609.07568); Indic guardrail resources (MuRIL,
AI4Bharat IndicLLMSuite / IndicAlign-Toxic, L3Cube-MahaHate).

Served-model figures require the cope-b / safeguard Modal endpoints (`serve_cope.py`,
`serve_safeguard.py`); both were stopped after the runs. Harness and per-run artefacts:
`apac-customization/` (`demo1_shieldstral`, `rung2_lora`, `demo3_cope`, `demo4_safeguard`,
`demo5_ideology`, `results/`).
